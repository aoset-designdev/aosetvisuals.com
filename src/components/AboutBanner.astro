---
// src/components/AboutBanner.astro
// Replace the placeholder with:
// import bannerImage from '../assets/about/about-banner.jpg';
// import { Image } from 'astro:assets';
---

<section class="relative w-full overflow-hidden" style="aspect-ratio: 980 / 650">
  <!--
    Replace with:
    <Image src={bannerImage} alt="AOSET remodeling project" class="h-full w-full object-cover"
      widths={[640, 980, 1851]} sizes="100vw" format="avif" quality={85} />
  -->
  <div class="h-full w-full bg-gradient-to-r from-[var(--color-ramp-700)] to-[var(--color-ramp-500)] flex items-center justify-center">
    <p class="text-white/60 text-sm">Place about-banner.jpg in src/assets/about/</p>
  </div>
</section>
